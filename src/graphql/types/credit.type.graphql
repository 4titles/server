type Credit {
    id: ID!
    category: CreditCategory!
    characters: [String!]
    episodesCount: Int
    name: Name!
    title: Title!
    createdAt: DateTime!
    updatedAt: DateTime!
}

enum CreditCategory {
    actor
    actress
    director
    writer
}
